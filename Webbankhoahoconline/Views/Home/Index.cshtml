@model IEnumerable<CourseModel>
<div class="col-sm-3">
    @Html.Partial("_SidebarPartial")
</div>

<div class="col-sm-9 padding-right">
    <div class="features_items">
        <h2 class="title text-center">Khóa học nổi bật</h2>
        @foreach (var item in Model)
        {
            <div class="col-sm-4">
                <div class="product-image-wrapper">
                    <a>
                    <div class="single-products">
                        <div class="productinfo text-center">
                            <img src="~/images/@item.ImageUrl" alt="@item.Name" width="100%" />
                            <h2>$@item.Price</h2>
                            <p>@item.Name</p>
                            <p>Danh mục : @item.Category.Name</p>
                            <p>Tên giảng viên : @item.Instructor.Name</p
                            <a class="btn btn-default add-to-cart"><i class="fa fa-shopping-cart"></i> Đăng ký ngay</a>
                        </div>
                    </div>
                    </a>
                    <div class="choose">
                        <ul class="nav nav-pills nav-justified">
                            <li><a href="#"><i class="fa fa-heart"></i> Yêu thích</a></li>
                            <li><a href="#"><i class="fa fa-exchange"></i> Thêm vào giỏ hàng</a></li>
                        </ul>
                    </div>
                </div>
            </div>}
            </div>
            </div>
        